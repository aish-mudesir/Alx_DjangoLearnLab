# LibraryProject

This Django project demonstrates:

- Custom User model with extra fields (`date_of_birth`, `profile_photo`)
- Book model with custom permissions: can_create, can_edit, can_delete
- Function-based views and permission-restricted views
- URL routing for book management

## Setup

1. Install dependencies:
```bash
pip install django
